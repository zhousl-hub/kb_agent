server:
  port: 8105
  servlet:
    context-path: /api/v1

spring:
  application:
    name: notification-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kba_notification?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: kba
    password: kba123456
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
  cloud:
    nacos:
      # 本地开发环境关闭 Nacos 服务发现
      discovery:
        enabled: false
